name: release

on:
  release:
    types: [created]

jobs:
  release:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package:
          - eslint-config-base
          - eslint-config-typescript
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 12
          registry-url: "https://npm.pkg.github.com"
          scope: "@vanyauhalin"
      - run: npm i -g pnpm
      - run: pnpm i --frozen-lockfile
      - run: pnpm build
      - run: pnpm publish packages/${{ matrix.package }} --no-git-checks; \
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
